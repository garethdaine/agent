{
    "name": "list-workspaces",
    "display_name": "List Workspaces",
    "description": "Retrieve a list of workspaces the authenticated user has access to.",
    "method": "GET",
    "path": "/workspaces",
    "parameters": {
        "type": "object",
        "properties": {
            "limit": {"type": "integer", "description": "Number of results to return (max 100)"},
            "offset": {"type": "string", "description": "Offset token for pagination"},
            "opt_fields": {"type": "string", "description": "Comma-separated list of optional fields to include"}
        }
    },
    "response_schema": {
        "type": "object",
        "properties": {
            "data": {"type": "array", "items": {"type": "object"}},
            "next_page": {"type": "object"}
        }
    },
    "requires_approval": false,
    "read_only": true,
    "idempotent": true,
    "pii_fields": []
}
