{
    "name": "list-configurations",
    "display_name": "List Configurations",
    "description": "Retrieve a list of configuration items (managed assets) from ConnectWise Manage.",
    "method": "GET",
    "path": "/company/configurations",
    "parameters": {
        "type": "object",
        "properties": {
            "conditions": {"type": "string", "description": "Filter conditions"},
            "orderBy": {"type": "string", "description": "Field to order results by"},
            "page": {"type": "integer", "description": "Page number for pagination"},
            "pageSize": {"type": "integer", "description": "Number of results per page (max 1000)"}
        }
    },
    "response_schema": {
        "type": "array",
        "items": {"type": "object"}
    },
    "requires_approval": false,
    "read_only": true,
    "idempotent": true,
    "pii_fields": []
}
