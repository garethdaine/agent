{
    "name": "list-customers",
    "display_name": "List Customers",
    "description": "Retrieve a list of customers from QuickBooks using a query.",
    "method": "GET",
    "path": "/query?query=SELECT * FROM Customer",
    "parameters": {
        "type": "object",
        "properties": {
            "query": {"type": "string", "description": "SQL-like query to filter customers (default: SELECT * FROM Customer)"},
            "startPosition": {"type": "integer", "description": "Starting position for pagination (1-based)"},
            "maxResults": {"type": "integer", "description": "Maximum number of results to return (max 1000)"}
        }
    },
    "response_schema": {
        "type": "object",
        "properties": {
            "QueryResponse": {
                "type": "object",
                "properties": {
                    "Customer": {"type": "array", "items": {"type": "object"}},
                    "startPosition": {"type": "integer"},
                    "maxResults": {"type": "integer"},
                    "totalCount": {"type": "integer"}
                }
            }
        }
    },
    "requires_approval": false,
    "read_only": true,
    "idempotent": true,
    "pii_fields": ["PrimaryEmailAddr.Address", "DisplayName", "PrimaryPhone.FreeFormNumber"]
}
