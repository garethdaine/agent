{
    "name": "update-lead",
    "display_name": "Update Lead",
    "description": "Update an existing lead record.",
    "method": "POST",
    "path": "/services/data/v59.0/sobjects/Lead/{id}",
    "parameters": {
        "type": "object",
        "properties": {
            "id": {"type": "string", "description": "Lead record ID"},
            "LastName": {"type": "string"},
            "FirstName": {"type": "string"},
            "Company": {"type": "string"},
            "Email": {"type": "string"},
            "Status": {"type": "string"}
        },
        "required": ["id"]
    },
    "response_schema": {"type": "object"},
    "requires_approval": true,
    "read_only": false,
    "idempotent": false,
    "pii_fields": ["LastName", "FirstName", "Email"]
}
