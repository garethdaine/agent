{
    "name": "list-job-orders",
    "display_name": "List Job Orders",
    "description": "Search and retrieve a list of job orders (vacancies) from the Bullhorn ATS.",
    "method": "GET",
    "path": "/search/JobOrder",
    "parameters": {
        "type": "object",
        "properties": {
            "query": {"type": "string", "description": "Lucene-style search query for filtering job orders"},
            "fields": {"type": "string", "description": "Comma-separated list of fields to return"},
            "count": {"type": "integer", "description": "Number of results to return"},
            "start": {"type": "integer", "description": "Starting index for pagination"}
        }
    },
    "response_schema": {
        "type": "object",
        "properties": {
            "data": {"type": "array", "items": {"type": "object"}},
            "total": {"type": "integer"},
            "count": {"type": "integer"}
        }
    },
    "requires_approval": false,
    "read_only": true,
    "idempotent": true,
    "pii_fields": []
}
