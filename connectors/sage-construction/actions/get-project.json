{
    "name": "get-project",
    "display_name": "Get Project",
    "description": "Retrieve detailed information about a specific construction project by its unique identifier.",
    "method": "GET",
    "path": "/projects/{projectId}",
    "parameters": {
        "type": "object",
        "properties": {
            "projectId": {"type": "string", "description": "The unique identifier of the project"}
        },
        "required": ["projectId"]
    },
    "response_schema": {
        "type": "object",
        "properties": {
            "project": {"type": "object"}
        }
    },
    "requires_approval": false,
    "read_only": true,
    "idempotent": true,
    "pii_fields": []
}
