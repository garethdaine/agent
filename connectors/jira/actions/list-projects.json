{
    "name": "list-projects",
    "display_name": "List Projects",
    "description": "Retrieve a list of projects from the Jira instance.",
    "method": "GET",
    "path": "/project",
    "parameters": {
        "type": "object",
        "properties": {
            "startAt": {"type": "integer", "description": "Index of the first item to return"},
            "maxResults": {"type": "integer", "description": "Maximum number of items to return (max 50)"},
            "orderBy": {"type": "string", "description": "Order results by a field (e.g. name, key)"},
            "expand": {"type": "string", "description": "Comma-separated list of fields to expand"}
        }
    },
    "response_schema": {
        "type": "array",
        "items": {"type": "object"}
    },
    "requires_approval": false,
    "read_only": true,
    "idempotent": true,
    "pii_fields": []
}
