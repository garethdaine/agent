{
    "name": "list-jobs",
    "display_name": "List Jobs",
    "description": "Retrieve a list of jobs from ServiceM8.",
    "method": "GET",
    "path": "/job.json",
    "parameters": {
        "type": "object",
        "properties": {
            "$filter": {"type": "string", "description": "OData filter expression (e.g. status eq 'Quote')"},
            "$top": {"type": "integer", "description": "Maximum number of records to return"},
            "$skip": {"type": "integer", "description": "Number of records to skip for pagination"}
        }
    },
    "response_schema": {
        "type": "array",
        "items": {"type": "object"}
    },
    "requires_approval": false,
    "read_only": true,
    "idempotent": true,
    "pii_fields": []
}
